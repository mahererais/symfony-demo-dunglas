"# symfony-demo-dunglas"

### Troubleshooting 

- first time i run `make composer c="require runtime/frankenphp-symfony"`
- on dev, run `make sf c="sass:build"`
- on prod, run `make sf c="asset-map:compile"`
